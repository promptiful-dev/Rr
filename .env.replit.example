# Replit Environment Configuration
# Copy this to Replit Secrets (not as a file, but as individual secrets)

# REQUIRED: Google Studio API Key
# Get this from: https://makersuite.google.com/app/apikey
# Add as secret named: GOOGLE_STUDIO_KEY
GOOGLE_STUDIO_KEY=AIza_your_google_studio_api_key_here

# Server Configuration (Replit will set PORT automatically)
NODE_ENV=production

# Rate Limiting (adjust based on your needs)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=60

# Logging
LOG_LEVEL=info

# Optional: Custom frontend URL for CORS (Replit will set this automatically)
# FRONTEND_URL=https://your-repl-name.username.repl.co

# Replit automatically sets these:
# REPLIT_DEPLOYMENT=true
# REPL_SLUG=your-repl-name  
# REPL_OWNER=your-username
# PORT=3000